#include "main.h"

int print_unsigned(va_list args)
{
	unsigned int n;
	int count;

	if (n == 0)
	{
		_putchar('0');
		return (1);
	}
	n = args(args, unsigned int);
	count = _printint(num);
	return (count);
}
